<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Photo Gallery</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 h-fit flex flex-col justify-center items-center">
    <header class="py-4 flex items-center justify-between w-full fixed top-0 bg-white">
        <h1 class="text-3xl font-bold text-center">Photo Gallery</h1>
        <div class="w-fit flex justify-end px-4">
            <a href="/" class="mt-4 text-blue-500 hover:underline trasition ease-in-out">Upload Image</a>
        </div>
    </header>
    
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4 mt-14">
        <% images.forEach(image => { %>
            <img src="data:<%= image.contentType %>;base64,<%= image.data.toString('base64') %>" alt="Image" class="rounded-lg shadow-md">
        <% }) %>
    </div>
</body>
</html>
